from setuptools import setup

setup(name = 'my_etl_app', packages = ['scripts'])
setup(name = 'my_featureengineering_app', packages = ['scripts'])
setup(name = 'my_modeltraining_app', packages = ['scripts'])